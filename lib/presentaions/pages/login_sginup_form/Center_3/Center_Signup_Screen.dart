

import 'package:flutter/material.dart';

class CenterSignupScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Institution Sign Up')),
      body: Center(child: Text('Institution Sign Up Form Here')),
    );
  }
}